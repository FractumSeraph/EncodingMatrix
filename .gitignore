# Large source and generated encode outputs
source.y4m

# Python bytecode cache
__pycache__/
*.pyc

# In-progress encodes (moved into the output folder once complete)
.partial/

# Temporary libvmaf JSON logs (written to the working dir during measurement)
vmaf_*.json
